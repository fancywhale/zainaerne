import { NgModule } from '@angular/core';
import { CSBackButton } from './back-button';
import { IonicModule } from 'ionic-angular';

@NgModule({
  declarations: [CSBackButton],
  imports: [IonicModule],
  exports: [CSBackButton],
})
export class CSBackButtonModule { }
